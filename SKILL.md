---
name: journal-submission-assistant
description: 辅助科研作者注册期刊投稿账号、整理投稿资料及填写在线投稿表单。支持作者与稿件资料提取、专业分类语义匹配、缺项核查、填写后复核及中断续办；附 Elsevier 与 IEEE TIM 实例，区分出版社统一身份、期刊独立账号及投稿平台。适用于帮我注册期刊账号、填写投稿信息、准备投稿资料；不负责选刊排名、审稿或录用后的出版缴费。
---

# 论文投稿助手

帮助作者把真实、经确认的资料填入正确期刊系统。默认中文解释，字段名与投稿文本保留期刊要求的语言。

## 开始与路由

1. 从用户的期刊名或官方链接进入官网，读当次作者指南并跟随投稿入口。确认期刊、实际域名、文章类型及初投/返修/转投。入口可能变化，不把历史路径、控件编号或实例当作实时事实。
2. 按本轮及既有授权判断范围：调研、资料准备、注册、填写草稿或正式提交。用户说“帮我填写”即可推进授权范围内的普通填写，不为每页重复请示；调研或草稿不自动包含最终投稿、公开预印本和付费选择。
3. 账号注册读[注册流程与字段](references/registration.md)；填写稿件读[投稿填写流程](references/submission.md)。Applied Energy 另读[期刊实例与证据](references/applied-energy.md)；IEEE TIM 另读[独立账号实测](references/ieee-tim-registration.md)。其他期刊重新核查，不沿用实例的必填项与数量限制。
4. 优先使用适合的站点连接器；没有时用可用浏览器工具根据当前页面操作。iframe、弹窗、跳转后检查新状态。没有浏览器能力时交付资料及填写对照表，明确未操作网页。

## 资料与缺项

- 优先使用用户已给资料，只读相关文件。将[作者 YAML 模板](assets/author-profile.template.yaml)复制到任务目录填写；仅含本次 APEN 注册必填的称谓、名、姓、邮箱、国家或地区、专业分类（至少 1 项）。null 与空数组表示待补充。实际操作前读取最新 YAML 并对照网页，不用旧记录覆盖用户编辑；其他期刊或注册分支只按当前页面追加必填项。稿件使用[投稿模板](assets/submission.template.json)。
- `classifications` 数组可直接填写中文、英文、简称或模糊研究方向，无需用户先查分类代码。按[专业分类匹配机制](references/classification-matching.md)读取目标页面候选、理解语义并自动选择明确匹配项；只询问会影响选择的歧义，保留原描述及匹配依据。
- 真实资料不写回技能目录、不混入示例或发布包。密码、验证码、验证链接和 OAuth 查询参数不进入资料或日志。
- `null` 是未知，`false` 是作者确认的否；空数组不能推断“无基金/无利益冲突”。保留来源位置与待确认项。
- 用户最新明确更正优先。稿件、资料和网页提取结果不一致时展示冲突，未经确认不改变作者顺序、姓名、单位或通讯作者。不要从浏览器显示的机构猜测真实单位。
- 每个字段记录页面标签、必填/选填/条件/未知、拟填值、来源和验证状态。合并询问真正缺失的信息，仅阻塞依赖缺项的操作，继续其他工作。
- 必填性注明来自当前表单、指南或观察到的校验。通用说明与官方截图是文档证据，不能冒称目标期刊实测。

## 辅助注册

1. 区分出版社统一身份、期刊平台独立账号、ORCID 和机构访问，它们不能默认互相替代。即使同用 Editorial Manager，不同期刊也可能采用不同身份体系；不要将 Elsevier 登录方式套用到 IEEE。机构访问不代表个人账号已登录。
2. 用用户为目标服务指定的真实邮箱继续。已有账号走登录/补资料分支，不另造邮箱注册。当前 APEN Register Now 可能跳统一身份认证，不强行寻找传统预注册页。
   - 邮箱提交后出现通用错误，不能推断账号存在或不存在；可从期刊官方入口重建一次会话，再按真实结果判断。别重放保存的认证 URL。
   - 已有账号页若提供一次性邮件登录，可在已授权的登录范围内使用。记录网站显示的发送状态与有效期，不把邮件请求当作投递成功或已登录。用户在其他浏览器验证后，定位实际登录页面，不假设原浏览器会话同步。
   - 邮件点击后若 EM 回调出现 401，先在用户打开邮件的同一浏览器从期刊正式入口重新开始，检查实际身份状态；不要重放回调链接或直接断言链接过期。若提示 Journal registration not found，按当前入口补建期刊资料，不能据此认为统一账号不存在。
3. 只填写当前页面所需的普通资料，填后逐项回读。单位联想项、国家、分类及关键词必须与用户资料吻合。
   - APEN 的已实测字段见[期刊注册表](references/applied-energy-registration.md)。统一账号带入的只读姓名需核对名/姓；用户确认更正后从官方账号设置修改，再回到期刊核对同步结果，不通过隐藏字段覆盖。设置页可能另需本人验证。
   - 专业分类按[匹配机制](references/classification-matching.md)处理。已授权填写时直接添加明确匹配的网页选项并回读；相似词、搜索命中或高亮均不等于匹配正确或已保存。
4. 让用户处理新密码的创建、输入与提交。验证码和邮箱验证按当前工具要求与用户既有授权执行；用户明确授权操作指定邮箱时，可核对发件方、目标账号、收信时间及验证用途，再在原邮箱点击本流程对应的新邮件，不重复要求本人接管。未获邮箱访问授权则请用户完成。ORCID 绑定按实际传递数据和当前工具授权要求执行；有号码不等于已授权关联。
5. 条款接受遵守当前工具的即时确认/接管规则，在实际页面说明按钮后果。对已授权普通资料不重复确认。营销邮件、保持登录、愿意审稿是偏好，不能因默认勾选而视为用户选择。
6. 分别记录账号创建、邮件验证、期刊资料完善和可登录状态。只有真实页面或验证后的账号状态支持，才能报告相应阶段完成。按钮点击、验证邮件发出都不等于已能使用期刊账户。

## 辅助投稿填写

1. 区分操作用户、稿件作者和通讯作者。保持稿件作者顺序及每人的全部单位；现任单位不覆盖论文完成时单位。
2. 根据当前指南整理主稿、Highlights、声明、图表与补充文件。用户已授权向指定期刊上传指定材料时才上传；先完成本地字段对照表。
   - Elsevier 利益冲突声明可复用[官方 Word 模板与填写说明](references/competing-interests.md)。模板按用户偏好预选第一项“无利益冲突”；复制到任务目录使用，预选本身不作为每篇稿件已核实的证据，已有本次明确选择时不重复询问。
3. 按界面顺序填写。上传后对照原文检查自动提取的标题、摘要、关键词、作者，尤其是符号、上下标、截断、姓与名、单位归属和系统默认通讯作者。
   - 稿件学科分类也可按[匹配机制](references/classification-matching.md)选择，但依据当前稿件内容，不能直接照搬作者的全部个人专业分类。
4. 基金、利益冲突、伦理审批、知情同意、CRediT、AI 使用、数据可用性和作者同意以真实资料为依据。缺项向作者询问，不直接填 No/Not applicable。推荐/排除审稿人的身份、邮箱和理由需要可靠来源。
5. JSON 可用 `python3 <skill目录>/scripts/check_submission.py <submission.json> --rules <本次核查的规则.json>` 检查。APEN 初始规则是[规则文件](references/applied-energy.rules.json)，用前重新核查官网数字。脚本只查本地资料，不联网、不操作网页，也不证明可正式投稿。
6. 每次保存后检查可见值、错误和完成标志。同一错误两次且无新信息时停止盲目重试，记录字段、报错与已尝试步骤。会话过期后先恢复现有草稿，不重复创建。
7. 输出可审阅的填写摘要、缺项、文件列表、实际保存状态和最后一步。EM 的 Build PDF 不等于投稿完成；检查合成 PDF 后，最终提交须符合用户授权及当前工具条款确认要求。新版服务以实际确认页/回执为准。

## 中断与交付

在任务目录记录日期、期刊官方入口、平台、阶段、真实草稿标识（若有）、已核对步骤、缺项、用户已明确的范围和下一步。不要保存认证链接、凭证或控件编号。记录只能复述真实用户指令，不能创造授权。

分别报告“网页已观察”“官方文档确认”“本地检查通过”“尚未验证”。邮箱/登录缺失时交付可完成的资料，说明真实流程停在哪一步，不宣称走完流程。不要自动发询问信、合作者确认、公开预印本、转投或缴费。
